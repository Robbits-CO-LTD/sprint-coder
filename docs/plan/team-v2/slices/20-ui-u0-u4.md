# UI Slices U0–U4

| Slice | Outcome | Legacy Picker | Gate |
|---|---|---|---|
| U0 | current UI／state／a11y仕様 | 維持 | 調査文書 |
| U1 | V2をflag OFF既定で追加 | 維持 | 1000件、仮想化、catalog-only |
| U2 | OFF／ON parity | 維持 | Chat／Team／restart／packaged |
| U3 | V2 default ON | fallback | 3OS、a11y、全E2E |
| U4 | legacy cleanup | 削除 | 独立PR、blocker 0 |

UI設計・実装は[UI Agent delegation process](../../../process/ui-agent-delegation.md)に従い、
`claude -p --model "$UI_DELEGATION_MODEL"`へSlice単位で委託する。backend contract、security、
test、merge判断はメインAgentが所有する。

## U0 completion

U0は2026-07-28にコード変更なしで完了した。Claude CLI委託は設定modelだけを使った
読み取り専用の2回目を採用し、変更0ファイル、差分0行、test未実行である。

- Mainの`settings.getRuntime`はCodex／Claudeをprobeし、選択Runtimeのmodel一覧を同じ
  `runtimeSettingsSchema` payloadで返す (`ipc.ts:354-387`)。
- RendererはTask非依存の単一`runtime` stateを持ち、model選択を楽観更新して失敗時に戻す
  (`appStore.ts:777-825`)。P1AのTask／Agent canonical selectionを読むRenderer IPCはまだない。
- Composer Pickerは`role="menu"`／`menuitemradio`とEscape closeを持つが、Arrow移動、
  roving tabindex、open時focus、close時focus restoreを持たない (`Composer.tsx:438-515`)。
- Settingsはnative selectへ全modelを同期renderする (`SettingsDialog.tsx:142-157`)。
- 現契約はmodel配列を32件へ制限する (`contracts/src/index.ts:1442-1467`)。Composer、
  Settingsとも全件renderで、検索、pagination、virtualization、revision indexがない。
- E2Eは固定model IDのclickとrestart復元を検査するが、Model PickerのArrow操作、
  active descendant、1000+ catalogを検査しない
  (`model-and-access-settings.spec.ts:20-44,105-125,165-180`)。

U1は既存`runtimeSettingsSchema.models`を拡張して使わず、Main-owned catalog queryとcanonical
selection IPCを先に追加する。Rendererのmembership判定でpage外modelを拒否してはならない。

## U1 progress

- U1a: Main-owned catalog revision／query／paging、1200件合成fixture、Task canonical selection IPC、
  task-aware legacy get/set、flag OFF既定のbackend contractを完了
- U1b: `UI_DELEGATION_MODEL`のClaude CLIでRendererのvirtualized Picker、Main側検索、
  cursor paging、keyboard、a11y、focus復元、不明値表示を実装。flag OFF／API不在時は旧Pickerを維持。
  desktop typecheck、対象file lint、virtualization unit 17件がgreen。全体E2Eはfinal gateへ留保
- U1c: `UI_DELEGATION_MODEL`のClaude CLIでSettingsへProvider Connection管理を統合。
  built-in CLIと外部APIの区別、OpenAI／OpenRouter／Anthropic／Gemini／xAI追加、接続状態、
  再確認、API key mask、Main-only Secret Storageの説明、同時verification防止を実装した。
  委託は許可4file、3往復、最終差分1000行未満に収め、メインAgentがsecurity／a11yをreviewした。
  component test 24件、対象file lint、`git diff --check`がgreen。fixture DOM上限のE2E証拠は、
  指示どおり全実装後のfinal gateへ留保する
