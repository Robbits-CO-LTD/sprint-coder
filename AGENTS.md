# 開発ワークフロー

このリポジトリで変更を行うときは、必ず以下の手順に従うこと。

1. 作業ブランチで変更し、必ずPull Request（PR）を作成する。
2. `main` ブランチへ直接pushしてはならない。ユーザーが明示的に直接pushを希望した場合のみ例外とする。
3. PR作成後、レビューBOTのレビュー結果を確認する。
4. レビューBOTから問題を指摘された場合は修正し、同じPRのブランチへcommitして再レビューを受ける。
5. 指摘が解消され、レビューで承認されたことを確認してから、PRをsquash mergeする。
