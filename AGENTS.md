# 開発ワークフロー

このリポジトリでは、変更リスクに応じてGit運用を切り替える。高リスク条件に一つでも該当する場合は、変更量にかかわらず高リスクフローを優先する。

## 通常フロー

次の変更は `main` へ直接commit・pushしてよい。

- 文言、UI微調整
- 明確な1〜数ファイルのバグ修正
- テスト追加、型修正
- 通常の機能追加

push前に必ずAI自身が差分をレビューし、lint、typecheck、変更範囲に関連するテストを実行する。フルCIは必須としない。

## 高リスクフロー

次の変更は作業ブランチで実装し、PRを作成する。

- DB・schema変更
- 認証・権限の変更
- ファイル操作に関わる変更
- updaterの変更
- release・build設定の変更
- 大規模refactor
- 複数OSに影響する変更
- 依存関係の大型更新

PR作成後はReviewBOTの指摘を確認・修正し、フルCIがgreenになってからmergeする。
